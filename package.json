{
  "name": "spark8co",
  "version": "1.0.0",
  "scripts": {
    "dev": "yarn vite",
    "build": "./build.sh",
    "serve": "yarn vite preview",
    "lint": "yarn lint:scss",
    "lint:scss": "yarn stylelint src/**/*.scss"
  },
  "devDependencies": {
    "sass": "^1.34.0",
    "stylelint": "^13.13.1",
    "stylelint-config-sass-guidelines": "^8.0.0",
    "vite": "^2.3.4"
  },
  "dependencies": {
    "bootstrap": "^4.6.0"
  }
}
